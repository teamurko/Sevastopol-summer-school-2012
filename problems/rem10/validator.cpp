#include "testlib.h"

using namespace std;

int main()
{
    registerValidation();
    int k = inf.readInt(1, 9);
    inf.readSpace();
    int n = inf.readInt(1, 100);
    inf.readEoln();
    inf.readEof();
    return 0;
}
